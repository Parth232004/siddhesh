# =================================================================================
# LOGISTICS MANAGER COMMUNICATION SERVICE - ENVIRONMENT CONFIGURATION
# =================================================================================
#
# This service provides multi-channel communication capabilities for logistics operations.
# It supports Email (Zoho), WhatsApp (Cloud API/Twilio), Telegram, and SMS (Twilio/Fast2SMS).
#
# REQUIRED VARIABLES:
# - PORT: Server port (default: 3000)
# - At least one communication provider must be configured
# - KARMA_TRACKER_BASE_URL and KARMA_TRACKER_API_KEY for karma tracking
#
# =================================================================================

# -------------------------------
# SERVER CONFIGURATION
# -------------------------------
# Port for the Express server to listen on
PORT=3000

# -------------------------------
# ZOHO MAIL CONFIGURATION (Email Service)
# -------------------------------
# Required for email functionality
ZOHO_SMTP_HOST=smtp.zoho.com
ZOHO_EMAIL=your-email@yourdomain.com
ZOHO_PASSWORD=your-zoho-password
# Optional: For Zoho Mail API (alternative to SMTP)
ZOHO_ACCESS_TOKEN=your-zoho-access-token

# -------------------------------
# WHATSAPP CONFIGURATION
# -------------------------------
# Option 1: WhatsApp Cloud API (Meta)
WHATSAPP_PHONE_NUMBER_ID=your-whatsapp-phone-number-id
WHATSAPP_ACCESS_TOKEN=your-whatsapp-access-token

# Option 2: Twilio WhatsApp (alternative)
TWILIO_WHATSAPP_NUMBER=+1234567890

# -------------------------------
# SMS CONFIGURATION
# -------------------------------
# Option 1: Twilio SMS
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890
# Optional: Status callback for delivery tracking
TWILIO_STATUS_CALLBACK_URL=https://your-domain.com/twilio/status

# Option 2: Fast2SMS (alternative provider)
FAST2SMS_API_KEY=your-fast2sms-api-key
FAST2SMS_SENDER_ID=YOURID

# -------------------------------
# TELEGRAM BOT CONFIGURATION
# -------------------------------
# Required for Telegram functionality
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# -------------------------------
# KARMA TRACKER CONFIGURATION (Required)
# -------------------------------
# Base URL for the Karma Tracker API
KARMA_TRACKER_BASE_URL=https://karma-tracker-api.example.com
# API key for authenticating with Karma Tracker
KARMA_TRACKER_API_KEY=your-karma-tracker-api-key

# -------------------------------
# KARMA POINTS CONFIGURATION (Optional - Defaults Provided)
# -------------------------------
# Points awarded for different communication types
KARMA_ORDER_UPDATE=2
KARMA_DELIVERY_ALERT=3
KARMA_CRM_ALERT=1
KARMA_QUICK_NOTIFICATION=1
KARMA_COMMAND_RESPONSE=2
KARMA_FALLBACK_UPDATE=1
KARMA_URGENT_UPDATE=4
KARMA_REPORT=2
KARMA_DEFAULT=1

# -------------------------------
# ADDITIONAL CONFIGURATION (Optional)
# -------------------------------
# BHIV Core API integration (if needed)
BHIV_CORE_API_URL=https://bhiv-core-api.example.com
BHIV_CORE_API_KEY=your-bhiv-core-api-key

# -------------------------------
# DEFAULT MESSAGE TYPES (Optional)
# -------------------------------
# Default types for different channels when not specified
DEFAULT_EMAIL_TYPE=transactional
DEFAULT_WHATSAPP_TYPE=delivery
DEFAULT_TELEGRAM_TYPE=notification
DEFAULT_SMS_TYPE=fallback

# =================================================================================
# SETUP INSTRUCTIONS:
# =================================================================================
#
# 1. Copy this file to .env in your project root
# 2. Configure at least one communication provider (Email, WhatsApp, SMS, or Telegram)
# 3. Set up Karma Tracker credentials
# 4. Configure API keys securely (never commit to version control)
# 5. Test configuration with: npm test
#
# =================================================================================